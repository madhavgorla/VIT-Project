name: code_rabbits_farmer_app
description: Offline-first parametric micro-insurance farmer mobile app for FS-2604.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.1.2
  http: ^1.5.0
  sqflite: ^2.4.1
  path: ^1.9.0
  connectivity_plus: ^6.1.5
  flutter_tts: ^4.2.3
  shared_preferences: ^2.5.3
  intl: ^0.20.2

  flutter_localizations:
    sdk: flutter
  sqflite_common_ffi_web: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  generate: true
